<!--  ReSharper disable IdentifierTypo  -->
<!--  ReSharper disable UnusedMember.Local  -->
<!--  ReSharper disable Xaml.ConstructorWarning  -->
<Page x:Class="CollapseLauncher.Pages.StarRailGameSettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:conv="using:CollapseLauncher.Pages"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:extension="using:CollapseLauncher.Extension"
      xmlns:helper="using:CollapseLauncher.Helper"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:static="using:CollapseLauncher.GameSettings.StarRail"
      CacheMode="BitmapCache"
      Loaded="InitializeSettings"
      NavigationCacheMode="Enabled"
      Unloaded="OnUnload"
      mc:Ignorable="d">
    <Page.Resources>
        <ThemeShadow x:Name="SharedShadow" />
        <conv:InverseBooleanConverter x:Key="BooleanInverse" />
    </Page.Resources>
    <Grid>
        <Grid x:Name="PageContent">
            <ScrollViewer x:Name="SettingsScrollViewer"
                          VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="32,40,32,32"
                            Padding="0,0,0,74">
                    <TextBlock Margin="0,0,0,16"
                               Style="{ThemeResource TitleLargeTextBlockStyle}"
                               Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_Title, Mode=OneWay}"
                               TextWrapping="Wrap" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0"
                                    Margin="0,0,32,0">
                            <StackPanel x:Name="GameResolutionPanel"
                                        Margin="0,16">
                                <StackPanel>
                                    <TextBlock Margin="0,0,0,16"
                                               Style="{ThemeResource SubtitleTextBlockStyle}"
                                               Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_ResolutionPanel, Mode=OneWay}"
                                               TextWrapping="Wrap" />
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition />
                                            <ColumnDefinition />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel x:Name="GameResolutionWindow"
                                                    Grid.Column="0"
                                                    Margin="0,0,0,8"
                                                    Orientation="Vertical">
                                            <Grid>
                                                <Border x:Name="VSyncTooltip"
                                                        Background="Transparent"
                                                        ToolTipService.ToolTip="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_VSync_Help, Mode=OneWay}" />
                                                <CheckBox x:Name="VSyncToggle"
                                                          HorizontalAlignment="Left"
                                                          VerticalAlignment="Center"
                                                          extension:UIElementExtensions.CursorType="Hand"
                                                          IsChecked="{x:Bind EnableVSync, Mode=TwoWay}">
                                                    <TextBlock Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_VSync, Mode=OneWay}"
                                                               TextWrapping="Wrap" />
                                                </CheckBox>
                                            </Grid>
                                            <CheckBox x:Name="GameResolutionFullscreen"
                                                      HorizontalAlignment="Left"
                                                      VerticalAlignment="Center"
                                                      extension:UIElementExtensions.CursorType="Hand"
                                                      IsChecked="{x:Bind IsFullscreenEnabled, Mode=TwoWay}">
                                                <TextBlock Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_Fullscreen, Mode=OneWay}"
                                                           TextWrapping="Wrap" />
                                            </CheckBox>
                                            <CheckBox x:Name="GameResolutionBorderless"
                                                      HorizontalAlignment="Left"
                                                      VerticalAlignment="Center"
                                                      extension:UIElementExtensions.CursorType="Hand"
                                                      IsChecked="{x:Bind IsBorderlessEnabled, Mode=TwoWay}">
                                                <TextBlock Text="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.Graphics_Borderless, Mode=OneWay}"
                                                           TextWrapping="Wrap" />
                                            </CheckBox>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition />
                                                </Grid.ColumnDefinitions>
                                                <CheckBox x:Name="GameWindowResizable"
                                                          HorizontalAlignment="Stretch"
                                                          VerticalAlignment="Center"
                                                          extension:UIElementExtensions.CursorType="Hand"
                                                          IsChecked="{x:Bind IsResizableWindow, Mode=TwoWay}"
                                                          IsEnabled="{x:Bind IsCanResizableWindow, Mode=OneWay}">
                                                    <TextBlock Text="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.Graphics_ResizableWindow, Mode=OneWay}"
                                                               TextWrapping="Wrap" />
                                                </CheckBox>
                                                <Button Grid.Column="1"
                                                        Width="24"
                                                        Height="24"
                                                        Margin="16,0,8,0"
                                                        Padding="0"
                                                        extension:UIElementExtensions.CursorType="Hand"
                                                        CornerRadius="4"
                                                        Style="{ThemeResource AcrylicButtonStyle}">
                                                    <Button.Content>
                                                        <FontIcon FontFamily="{ThemeResource FontAwesome}"
                                                                  FontSize="10"
                                                                  Glyph="&#x3f;" />
                                                    </Button.Content>
                                                    <Button.Flyout>
                                                        <Flyout>
                                                            <TextBlock MaxWidth="360"
                                                                       FontWeight="SemiBold"
                                                                       Text="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.Graphics_ResizableWindowTooltip, Mode=OneWay}"
                                                                       TextAlignment="Center"
                                                                       TextWrapping="Wrap" />
                                                        </Flyout>
                                                    </Button.Flyout>
                                                </Button>
                                            </Grid>
                                            <!--
                                                Exclusive Fullscreen option is disabled in Honkai:Star Rail due to it being ignored by the game
                                                Delete `Visibility="Collapsed"` to revert this change
                                            -->
                                            <CheckBox x:Name="GameResolutionFullscreenExclusive"
                                                      HorizontalAlignment="Left"
                                                      VerticalAlignment="Center"
                                                      extension:UIElementExtensions.CursorType="Hand"
                                                      IsChecked="{x:Bind IsExclusiveFullscreenEnabled, Mode=TwoWay}"
                                                      IsEnabled="{x:Bind IsCanExclusiveFullscreen, Mode=OneWay}"
                                                      Visibility="Collapsed">
                                                <TextBlock Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_ExclusiveFullscreen, Mode=OneWay}"
                                                           TextWrapping="Wrap" />
                                            </CheckBox>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1"
                                                    Margin="4,0,0,0">
                                            <CheckBox x:Name="MobileModeToggle"
                                                      HorizontalAlignment="Left"
                                                      VerticalAlignment="Center"
                                                      extension:UIElementExtensions.CursorType="Hand"
                                                      IsChecked="{x:Bind IsMobileMode, Mode=TwoWay}"
                                                      IsEnabled="False"
                                                      ToolTipService.ToolTip="{x:Bind helper:Locale.Current.Lang._Misc.Generic_GameFeatureDeprecation, Mode=OneWay}">
                                                <TextBlock x:Name="MobileModeToggleText"
                                                           TextWrapping="Wrap" />
                                                <!--  Text for this moved to LoadPage()  -->
                                            </CheckBox>
                                        </StackPanel>
                                    </Grid>
                                    <Grid Margin="0,0,0,16">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition />
                                        </Grid.ColumnDefinitions>
                                        <ComboBox x:Name="GameResolutionSelector"
                                                  MinWidth="128"
                                                  VerticalAlignment="Center"
                                                  extension:UIElementExtensions.CursorType="Hand"
                                                  CornerRadius="14"
                                                  IsEnabled="{x:Bind IsCustomResolutionEnabled, Mode=OneWay, Converter={StaticResource BooleanInverse}}"
                                                  PlaceholderText="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_ResSelectPlaceholder, Mode=OneWay}"
                                                  SelectedItem="{x:Bind ResolutionSelected, Mode=TwoWay}" />
                                        <Grid Grid.Column="1"
                                              HorizontalAlignment="Stretch"
                                              VerticalAlignment="Center">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition />
                                            </Grid.ColumnDefinitions>
                                            <CheckBox x:Name="GameCustomResolutionCheckbox"
                                                      Margin="16,0,16,0"
                                                      VerticalAlignment="Center"
                                                      extension:UIElementExtensions.CursorType="Hand"
                                                      IsChecked="{x:Bind IsCustomResolutionEnabled, Mode=TwoWay}"
                                                      IsEnabled="{x:Bind IsCanCustomResolution, Mode=OneWay}">
                                                <TextBlock Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_ResCustom, Mode=OneWay}"
                                                           TextWrapping="Wrap" />
                                            </CheckBox>
                                            <Button Grid.Column="1"
                                                    Width="24"
                                                    Height="24"
                                                    Margin="0,0,8,0"
                                                    Padding="0"
                                                    extension:UIElementExtensions.CursorType="Hand"
                                                    CornerRadius="4"
                                                    Style="{ThemeResource AcrylicButtonStyle}">
                                                <Button.Content>
                                                    <FontIcon FontFamily="{ThemeResource FontAwesome}"
                                                              FontSize="10"
                                                              Glyph="&#x3f;" />
                                                </Button.Content>
                                                <Button.Flyout>
                                                    <Flyout>
                                                        <TextBlock MaxWidth="360"
                                                                   FontWeight="SemiBold"
                                                                   Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_ResCustomTooltip, Mode=OneWay}"
                                                                   TextAlignment="Center"
                                                                   TextWrapping="Wrap" />
                                                    </Flyout>
                                                </Button.Flyout>
                                            </Button>
                                        </Grid>
                                    </Grid>
                                </StackPanel>
                                <StackPanel x:Name="GameCustomResolutionPanel"
                                            Orientation="Horizontal">
                                    <TextBlock Margin="0,0,8,0"
                                               VerticalAlignment="Center"
                                               Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_ResCustomW, Mode=OneWay}" />
                                    <NumberBox x:Name="GameCustomResolutionWidth"
                                               Width="100"
                                               HorizontalAlignment="Left"
                                               CornerRadius="8,8,0,0"
                                               IsEnabled="{x:Bind IsCanResolutionWH, Mode=OneWay}"
                                               Value="{x:Bind ResolutionW, Mode=TwoWay}" />
                                    <TextBlock Margin="16,0,8,0"
                                               VerticalAlignment="Center"
                                               Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_ResCustomH, Mode=OneWay}" />
                                    <NumberBox x:Name="GameCustomResolutionHeight"
                                               Width="100"
                                               HorizontalAlignment="Left"
                                               CornerRadius="8,8,0,0"
                                               IsEnabled="{x:Bind IsCanResolutionWH, Mode=OneWay}"
                                               Value="{x:Bind ResolutionH, Mode=TwoWay}" />
                                </StackPanel>
                                <TextBlock Margin="0,16,8,0"
                                           Style="{ThemeResource SubtitleTextBlockStyle}"
                                           Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_FPS, Mode=OneWay}"
                                           TextWrapping="Wrap" />
                                <TextBlock Margin="0,4,0,0"
                                           Style="{ThemeResource CaptionTextBlockStyle}"
                                           Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_FPS_Help, Mode=OneWay}"
                                           TextWrapping="Wrap" />
                                <TextBlock Margin="0,4,0,8"
                                           Style="{ThemeResource CaptionTextBlockStyle}"
                                           Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_FPS_Help2, Mode=OneWay}"
                                           TextWrapping="Wrap" />
                                <ComboBox x:Name="FPSSelector"
                                          MinWidth="128"
                                          Margin="0,8,0,0"
                                          extension:UIElementExtensions.CursorType="Hand"
                                          CornerRadius="14"
                                          ItemsSource="{x:Bind static:Model.FpsIndex}"
                                          SelectedIndex="{x:Bind FPS, Mode=TwoWay}" />
                            </StackPanel>
                            <StackPanel x:Name="GameBoostPanel"
                                        Orientation="Horizontal">
                                <ToggleSwitch Margin="4,0,0,8"
                                              extension:UIElementExtensions.CursorType="Hand"
                                              Header="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.GameBoost, Mode=OneWay}"
                                              IsOn="{x:Bind IsGameBoost, Mode=TwoWay}"
                                              OffContent="{x:Bind helper:Locale.Current.Lang._Misc.Disabled, Mode=OneWay}"
                                              OnContent="{x:Bind helper:Locale.Current.Lang._Misc.Enabled, Mode=OneWay}" />
                            </StackPanel>
                        </StackPanel>
                        <StackPanel x:Name="StarRailGameGraphicsPanel"
                                    Grid.Column="1"
                                    Margin="0,16">
                            <TextBlock Margin="0,0,0,16"
                                       Style="{ThemeResource SubtitleTextBlockStyle}"
                                       Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_SpecPanel, Mode=OneWay}"
                                       TextWrapping="Wrap" />
                            <StackPanel>
                                <StackPanel Margin="0,0,16,8">
                                    <Grid x:Name="RenderScaleGrid">
                                        <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}"
                                                   Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_RenderScale, Mode=OneWay}"
                                                   TextWrapping="Wrap" />
                                    </Grid>
                                    <Slider x:Name="RenderScaleSlider"
                                            Maximum="2.0"
                                            Minimum="0.6"
                                            StepFrequency="0.2"
                                            Style="{ThemeResource FatSliderStyle}"
                                            TickFrequency="0.2"
                                            TickPlacement="Inline"
                                            Value="{x:Bind RenderScale, Mode=TwoWay}" />
                                </StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0"
                                                VerticalAlignment="Bottom">
                                        <TextBlock MaxWidth="128"
                                                   Margin="0,0,0,8"
                                                   HorizontalAlignment="Left"
                                                   Style="{ThemeResource BodyStrongTextBlockStyle}"
                                                   Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_ShadowQuality, Mode=OneWay}"
                                                   TextWrapping="Wrap" />
                                        <ComboBox x:Name="ShadowQualitySelector"
                                                  Margin="0,0,16,8"
                                                  HorizontalAlignment="Stretch"
                                                  extension:UIElementExtensions.CursorType="Hand"
                                                  CornerRadius="14"
                                                  SelectedIndex="{x:Bind ShadowQuality, Mode=TwoWay}">
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecDisabled, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecVeryLow, Mode=OneWay}"
                                                          IsEnabled="False"
                                                          Visibility="Collapsed" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecLow, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecMedium, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecHigh, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecVeryHigh, Mode=OneWay}"
                                                          IsEnabled="False"
                                                          Visibility="Collapsed" />
                                        </ComboBox>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1"
                                                VerticalAlignment="Bottom">
                                        <TextBlock MaxWidth="128"
                                                   Margin="0,0,0,8"
                                                   HorizontalAlignment="Left"
                                                   Style="{ThemeResource BodyStrongTextBlockStyle}"
                                                   Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_LightQuality, Mode=OneWay}"
                                                   TextWrapping="Wrap" />
                                        <ComboBox x:Name="LightQualitySelector"
                                                  Margin="0,0,16,8"
                                                  HorizontalAlignment="Stretch"
                                                  extension:UIElementExtensions.CursorType="Hand"
                                                  CornerRadius="14"
                                                  SelectedIndex="{x:Bind LightQuality, Mode=TwoWay}">
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecDisabled, Mode=OneWay}"
                                                          IsEnabled="False"
                                                          Visibility="Collapsed" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecVeryLow, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecLow, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecMedium, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecHigh, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecVeryHigh, Mode=OneWay}" />
                                        </ComboBox>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2"
                                                VerticalAlignment="Bottom">
                                        <TextBlock MaxWidth="128"
                                                   Margin="0,0,0,8"
                                                   HorizontalAlignment="Left"
                                                   Style="{ThemeResource BodyStrongTextBlockStyle}"
                                                   Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_CharacterQuality, Mode=OneWay}"
                                                   TextWrapping="Wrap" />
                                        <ComboBox x:Name="CharacterQualitySelector"
                                                  Margin="0,0,16,8"
                                                  HorizontalAlignment="Stretch"
                                                  extension:UIElementExtensions.CursorType="Hand"
                                                  CornerRadius="14"
                                                  SelectedIndex="{x:Bind CharacterQuality, Mode=TwoWay}">
                                            <!--  dum workaround for the dum enum  -->
                                            <ComboBoxItem Content="0"
                                                          IsEnabled="False"
                                                          Visibility="Collapsed" />
                                            <ComboBoxItem Content="1"
                                                          IsEnabled="False"
                                                          Visibility="Collapsed" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecLow, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecMedium, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecHigh, Mode=OneWay}" />
                                        </ComboBox>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0"
                                                VerticalAlignment="Bottom">
                                        <TextBlock MaxWidth="128"
                                                   Margin="0,8,0,8"
                                                   HorizontalAlignment="Left"
                                                   Style="{ThemeResource BodyStrongTextBlockStyle}"
                                                   Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_EnvDetailQuality, Mode=OneWay}"
                                                   TextWrapping="Wrap" />
                                        <ComboBox x:Name="EnvDetailQualitySelector"
                                                  Margin="0,0,16,8"
                                                  HorizontalAlignment="Stretch"
                                                  extension:UIElementExtensions.CursorType="Hand"
                                                  CornerRadius="14"
                                                  SelectedIndex="{x:Bind EnvDetailQuality, Mode=TwoWay}">
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecDisabled, Mode=OneWay}"
                                                          IsEnabled="False"
                                                          Visibility="Collapsed" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecVeryLow, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecLow, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecMedium, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecHigh, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecVeryHigh, Mode=OneWay}" />
                                        </ComboBox>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1"
                                                VerticalAlignment="Bottom">
                                        <TextBlock MaxWidth="128"
                                                   Margin="0,8,0,8"
                                                   HorizontalAlignment="Left"
                                                   Style="{ThemeResource BodyStrongTextBlockStyle}"
                                                   Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_ReflectionQuality, Mode=OneWay}"
                                                   TextWrapping="Wrap" />
                                        <ComboBox x:Name="ReflectionQualitySelector"
                                                  Margin="0,0,16,8"
                                                  HorizontalAlignment="Stretch"
                                                  extension:UIElementExtensions.CursorType="Hand"
                                                  CornerRadius="14"
                                                  SelectedIndex="{x:Bind ReflectionQuality, Mode=TwoWay}">
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecDisabled, Mode=OneWay}"
                                                          IsEnabled="False"
                                                          Visibility="Collapsed" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecVeryLow, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecLow, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecMedium, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecHigh, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecVeryHigh, Mode=OneWay}" />
                                        </ComboBox>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2"
                                                VerticalAlignment="Bottom">
                                        <TextBlock MaxWidth="128"
                                                   Margin="0,8,0,8"
                                                   HorizontalAlignment="Left"
                                                   Style="{ThemeResource BodyStrongTextBlockStyle}"
                                                   Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_BloomQuality, Mode=OneWay}"
                                                   TextWrapping="Wrap" />
                                        <ComboBox x:Name="BloomQualitySelector"
                                                  Margin="0,0,16,8"
                                                  HorizontalAlignment="Stretch"
                                                  extension:UIElementExtensions.CursorType="Hand"
                                                  CornerRadius="14"
                                                  SelectedIndex="{x:Bind BloomQuality, Mode=TwoWay}">
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecDisabled, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecVeryLow, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecLow, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecMedium, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecHigh, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecVeryHigh, Mode=OneWay}" />
                                        </ComboBox>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0"
                                                VerticalAlignment="Bottom">
                                        <TextBlock MaxWidth="128"
                                                   Margin="0,8,0,8"
                                                   HorizontalAlignment="Left"
                                                   Style="{ThemeResource BodyStrongTextBlockStyle}"
                                                   Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_AAMode, Mode=OneWay}"
                                                   TextWrapping="Wrap" />
                                        <ComboBox x:Name="AAModeSelector"
                                                  Margin="0,0,16,8"
                                                  HorizontalAlignment="Stretch"
                                                  extension:UIElementExtensions.CursorType="Hand"
                                                  CornerRadius="14"
                                                  SelectedIndex="{x:Bind AAMode, Mode=TwoWay}">
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecDisabled, Mode=OneWay}" />
                                            <ComboBoxItem Content="TAA" />
                                            <ComboBoxItem Content="FXAA" />
                                        </ComboBox>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2"
                                                VerticalAlignment="Bottom"
                                                Visibility="Collapsed">
                                        <TextBlock MaxWidth="128"
                                                   Margin="0,8,0,8"
                                                   HorizontalAlignment="Left"
                                                   Style="{ThemeResource BodyStrongTextBlockStyle}"
                                                   Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_ResolutionQuality, Mode=OneWay}"
                                                   TextWrapping="Wrap" />
                                        <ComboBox x:Name="ResolutionQualitySelector"
                                                  Margin="0,0,16,8"
                                                  HorizontalAlignment="Stretch"
                                                  extension:UIElementExtensions.CursorType="Hand"
                                                  CornerRadius="14"
                                                  SelectedIndex="{x:Bind ResolutionQuality, Mode=TwoWay}">
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecDisabled, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecVeryLow, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecLow, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecMedium, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecHigh, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecVeryHigh, Mode=OneWay}" />
                                        </ComboBox>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1"
                                                VerticalAlignment="Bottom">
                                        <TextBlock MaxWidth="128"
                                                   Margin="0,8,0,8"
                                                   HorizontalAlignment="Left"
                                                   Style="{ThemeResource BodyStrongTextBlockStyle}"
                                                   Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_SFXQuality, Mode=OneWay}"
                                                   TextWrapping="Wrap" />
                                        <ComboBox x:Name="SFXQualitySelector"
                                                  Margin="0,0,16,8"
                                                  HorizontalAlignment="Stretch"
                                                  extension:UIElementExtensions.CursorType="Hand"
                                                  CornerRadius="14"
                                                  SelectedIndex="{x:Bind SFXQuality, Mode=TwoWay}">
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecDisabled, Mode=OneWay}"
                                                          IsEnabled="False"
                                                          Visibility="Collapsed" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecVeryLow, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecLow, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecMedium, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecHigh, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecVeryHigh, Mode=OneWay}"
                                                          IsEnabled="False"
                                                          Visibility="Collapsed" />
                                        </ComboBox>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2"
                                                VerticalAlignment="Bottom">
                                        <TextBlock MaxWidth="128"
                                                   Margin="0,8,0,8"
                                                   HorizontalAlignment="Left"
                                                   Style="{ThemeResource BodyStrongTextBlockStyle}"
                                                   Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_DlssQuality, Mode=OneWay}"
                                                   TextWrapping="Wrap" />
                                        <ComboBox x:Name="DlssQualitySelector"
                                                  Margin="0,0,16,8"
                                                  HorizontalAlignment="Stretch"
                                                  extension:UIElementExtensions.CursorType="Hand"
                                                  CornerRadius="14"
                                                  SelectedIndex="{x:Bind DlssQuality, Mode=TwoWay}">
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.SpecDisabled, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_DLSS_UHP, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_DLSS_Perf, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_DLSS_Balanced, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_DLSS_Quality, Mode=OneWay}" />
                                            <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_DLSS_DLAA, Mode=OneWay}" />
                                        </ComboBox>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                            <StackPanel>
                                <Grid ColumnSpacing="16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>
                                    <CheckBox x:Name="ToggleSelfShadow"
                                              Grid.Column="0"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"
                                              extension:UIElementExtensions.CursorType="Hand"
                                              IsChecked="{x:Bind SelfShadow, Mode=TwoWay}">
                                        <TextBlock Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_SelfShadow, Mode=OneWay}"
                                                   TextWrapping="WrapWholeWords" />
                                    </CheckBox>
                                    <CheckBox x:Name="ToggleHalfResTransparency"
                                              Grid.Column="1"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"
                                              extension:UIElementExtensions.CursorType="Hand"
                                              IsChecked="{x:Bind HalfResTransparent, Mode=TwoWay}">
                                        <TextBlock Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Graphics_HalfResTransparent, Mode=OneWay}"
                                                   TextWrapping="WrapWholeWords" />
                                    </CheckBox>
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                    <MenuFlyoutSeparator Margin="0,4,0,8" />
                    <TextBlock Margin="0,0,0,16"
                               Style="{ThemeResource TitleLargeTextBlockStyle}"
                               Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Audio_Title, Mode=OneWay}"
                               TextWrapping="Wrap" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <StackPanel x:Name="AudioSettingsPanelLeft"
                                        Margin="0,16,64,16">
                                <StackPanel>
                                    <TextBlock Margin="0,0,0,16"
                                               Style="{ThemeResource SubtitleTextBlockStyle}"
                                               Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Audio_Master, Mode=OneWay}"
                                               TextWrapping="Wrap" />
                                    <StackPanel Margin="0,0,32,8"
                                                Orientation="Vertical">
                                        <Slider x:Name="AudioMasterVolumeSlider"
                                                Maximum="10"
                                                Style="{ThemeResource FatSliderStyle}"
                                                TickFrequency="1"
                                                TickPlacement="Outside"
                                                Value="{x:Bind AudioMasterVolume, Mode=TwoWay}" />
                                    </StackPanel>
                                    <TextBlock Margin="0,16,0,16"
                                               Style="{ThemeResource SubtitleTextBlockStyle}"
                                               Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Audio_SFX, Mode=OneWay}"
                                               TextWrapping="Wrap" />
                                    <StackPanel Margin="0,0,32,8"
                                                Orientation="Vertical">
                                        <Slider x:Name="AudioSFXVolumeSlider"
                                                Maximum="10"
                                                Style="{ThemeResource FatSliderStyle}"
                                                TickFrequency="1"
                                                TickPlacement="Outside"
                                                Value="{x:Bind AudioSFXVolume, Mode=TwoWay}" />
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <StackPanel x:Name="AudioSettingsPanelRight"
                                        Margin="0,16,64,16">
                                <StackPanel>
                                    <TextBlock Margin="0,0,0,16"
                                               Style="{ThemeResource SubtitleTextBlockStyle}"
                                               Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Audio_BGM, Mode=OneWay}"
                                               TextWrapping="Wrap" />
                                    <StackPanel Margin="0,0,32,8"
                                                Orientation="Vertical">
                                        <Slider x:Name="AudioBGMVolumeSlider"
                                                Maximum="10"
                                                Style="{ThemeResource FatSliderStyle}"
                                                TickFrequency="1"
                                                TickPlacement="Outside"
                                                Value="{x:Bind AudioBGMVolume, Mode=TwoWay}" />
                                    </StackPanel>
                                    <TextBlock Margin="0,16,0,16"
                                               Style="{ThemeResource SubtitleTextBlockStyle}"
                                               Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Audio_VO, Mode=OneWay}"
                                               TextWrapping="Wrap" />
                                    <StackPanel Margin="0,0,32,8"
                                                Orientation="Vertical">
                                        <Slider x:Name="AudioVOVolumeSlider"
                                                Maximum="10"
                                                Style="{ThemeResource FatSliderStyle}"
                                                TickFrequency="1"
                                                TickPlacement="Outside"
                                                Value="{x:Bind AudioVOVolume, Mode=TwoWay}" />
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                    <MenuFlyoutSeparator Margin="0,4,0,8" />
                    <StackPanel>
                        <TextBlock Margin="0,0,0,16"
                                   Style="{ThemeResource TitleLargeTextBlockStyle}"
                                   Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Language, Mode=OneWay}"
                                   TextWrapping="Wrap" />
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Row="0"
                                       Margin="0,0,0,16"
                                       TextWrapping="Wrap">
                                <Run Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Language_Help1, Mode=OneWay}" />
                                <LineBreak />
                                <Run Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.Language_Help2, Mode=OneWay}" />
                            </TextBlock>
                            <Grid Grid.Row="1"
                                  Grid.Column="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200" />
                                    <ColumnDefinition Width="200" />
                                </Grid.ColumnDefinitions>
                                <StackPanel x:Name="LangSettingLeft"
                                            Grid.Column="0"
                                            Margin="0,0,24,0">
                                    <TextBlock Margin="0,0,0,8"
                                               FontSize="16"
                                               Style="{ThemeResource BodyStrongTextBlockStyle}"
                                               Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.LanguageAudio, Mode=OneWay}" />
                                    <ComboBox x:Name="AudioLangSelector"
                                              Margin="-4,0,0,16"
                                              HorizontalAlignment="Stretch"
                                              extension:UIElementExtensions.CursorType="Hand"
                                              CornerRadius="14"
                                              SelectedIndex="{x:Bind AudioLang, Mode=TwoWay}">
                                        <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.VO_en, Mode=OneWay}" />
                                        <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.VO_jp, Mode=OneWay}" />
                                        <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.VO_cn, Mode=OneWay}" />
                                        <ComboBoxItem Content="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.VO_kr, Mode=OneWay}" />
                                    </ComboBox>
                                </StackPanel>
                                <StackPanel x:Name="LangSettingRight"
                                            Grid.Column="1"
                                            Margin="0,0,24,0">
                                    <TextBlock Margin="0,0,0,8"
                                               FontSize="16"
                                               Style="{ThemeResource BodyStrongTextBlockStyle}"
                                               Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.LanguageText, Mode=OneWay}" />
                                    <ComboBox x:Name="TextLangSelector"
                                              Margin="-4,0,0,16"
                                              HorizontalAlignment="Stretch"
                                              extension:UIElementExtensions.CursorType="Hand"
                                              CornerRadius="14"
                                              MaxDropDownHeight="200"
                                              SelectedIndex="{x:Bind TextLang, Mode=TwoWay}">
                                        <ComboBoxItem Content="English" />
                                        <ComboBoxItem Content="日本語" />
                                        <ComboBoxItem Content="简体中文" />
                                        <ComboBoxItem Content="繁體中文" />
                                        <ComboBoxItem Content="한국어" />
                                        <ComboBoxItem Content="Español" />
                                        <ComboBoxItem Content="Русский" />
                                        <ComboBoxItem Content="ภาษาไทย" />
                                        <ComboBoxItem Content="Tiếng Việt" />
                                        <ComboBoxItem Content="Bahasa Indonesia" />
                                        <ComboBoxItem Content="Français" />
                                        <ComboBoxItem Content="Deutsch" />
                                        <ComboBoxItem Content="Português" />
                                    </ComboBox>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </StackPanel>
                    <MenuFlyoutSeparator Margin="0,4,0,8" />
                    <TextBlock Margin="0,0,0,16"
                               Style="{ThemeResource TitleLargeTextBlockStyle}"
                               Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.CustomArgs_Title, Mode=OneWay}" />
                    <TextBlock Margin="0,0,0,0"
                               Style="{ThemeResource SubtitleTextBlockStyle}"
                               Text="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.CustomArgs_Subtitle, Mode=OneWay}" />
                    <ToggleSwitch Margin="0,0,0,16"
                                  extension:UIElementExtensions.CursorType="Hand"
                                  IsOn="{x:Bind IsUseCustomArgs, Mode=TwoWay}"
                                  OffContent="{x:Bind helper:Locale.Current.Lang._Misc.Disabled, Mode=OneWay}"
                                  OnContent="{x:Bind helper:Locale.Current.Lang._Misc.Enabled, Mode=OneWay}" />
                    <TextBox x:Name="CustomArgsTextBox"
                             Margin="0,0,0,16"
                             HorizontalAlignment="Stretch"
                             CornerRadius="8,8,0,0"
                             Text="{x:Bind CustomArgsValue, Mode=TwoWay}" />
                    <TextBlock Height="Auto"
                               TextWrapping="WrapWholeWords">
                        <Run Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.CustomArgs_Footer1, Mode=OneWay}" />
                        <Hyperlink extension:UIElementExtensions.CursorType="Hand"
                                   NavigateUri="https://docs.unity3d.com/Manual/PlayerCommandLineArguments.html"
                                   UnderlineStyle="None">
                            <Run FontWeight="Bold"
                                 Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.CustomArgs_Footer2, Mode=OneWay}" />
                        </Hyperlink>
                        <Run Text="{x:Bind helper:Locale.Current.Lang._StarRailGameSettingsPage.CustomArgs_Footer3, Mode=OneWay}" />
                    </TextBlock>
                    <MenuFlyoutSeparator Margin="0,16,0,16" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Margin="0,0,8,0"
                                   VerticalAlignment="Stretch"
                                   Style="{ThemeResource TitleLargeTextBlockStyle}"
                                   Text="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.Advanced_Title, Mode=OneWay}" />
                        <ToggleSwitch Grid.Column="1"
                                      Margin="8,12,0,8"
                                      VerticalAlignment="Stretch"
                                      VerticalContentAlignment="Stretch"
                                      extension:UIElementExtensions.CursorType="Hand"
                                      FontSize="26"
                                      FontWeight="SemiBold"
                                      IsOn="{x:Bind IsUseAdvancedSettings, Mode=TwoWay}"
                                      OffContent="{x:Bind helper:Locale.Current.Lang._Misc.Disabled, Mode=OneWay}"
                                      OnContent="{x:Bind helper:Locale.Current.Lang._Misc.Enabled, Mode=OneWay}" />
                    </Grid>
                    <StackPanel x:Name="AdvancedSettingsPanel"
                                Visibility="Collapsed">
                        <TextBlock Margin="0,0,0,8"
                                   TextWrapping="WrapWholeWords">
                            <Run Text="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.Advanced_Subtitle1, Mode=OneWay}" />
                            <Run FontWeight="Bold"
                                 Text="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.Advanced_Subtitle2, Mode=OneWay}" />
                            <Run Text="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.Advanced_Subtitle3, Mode=OneWay}" />
                        </TextBlock>
                        <TextBlock Margin="0,0,8,8"
                                   Style="{ThemeResource SubtitleTextBlockStyle}"
                                   Text="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.Advanced_GLC_PreLaunch_Title, Mode=OneWay}" />
                        <ToggleSwitch Margin="0,0,0,0"
                                      extension:UIElementExtensions.CursorType="Hand"
                                      Header="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.Advanced_GLC_PreLaunch_Subtitle, Mode=OneWay}"
                                      IsOn="{x:Bind IsUsePreLaunchCommand, Mode=TwoWay}"
                                      OffContent="{x:Bind helper:Locale.Current.Lang._Misc.Disabled, Mode=OneWay}"
                                      OnContent="{x:Bind helper:Locale.Current.Lang._Misc.Enabled, Mode=OneWay}" />
                        <ToggleSwitch x:Name="PreLaunchForceCloseToggle"
                                      Margin="0,0,0,0"
                                      extension:UIElementExtensions.CursorType="Hand"
                                      Header="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.Advanced_GLC_PreLaunch_Exit, Mode=OneWay}"
                                      IsOn="{x:Bind IsPreLaunchCommandExitOnGameClose, Mode=TwoWay}"
                                      OffContent="{x:Bind helper:Locale.Current.Lang._Misc.Disabled, Mode=OneWay}"
                                      OnContent="{x:Bind helper:Locale.Current.Lang._Misc.Enabled, Mode=OneWay}" />
                        <NumberBox x:Name="GameLaunchDelay"
                                   Width="200"
                                   Margin="0,0,0,12"
                                   HorizontalAlignment="Left"
                                   Header="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.Advanced_GLC_PreLaunch_Delay, Mode=OneWay}"
                                   ValueChanged="GameLaunchDelay_OnValueChanged"
                                   Value="{x:Bind LaunchDelay, Mode=TwoWay}" />
                        <TextBox x:Name="PreLaunchCommandTextBox"
                                 Margin="0,0,0,4"
                                 HorizontalAlignment="Stretch"
                                 CornerRadius="8,8,0,0"
                                 IsSpellCheckEnabled="False"
                                 Text="{x:Bind PreLaunchCommand, Mode=TwoWay}" />
                        <TextBlock Margin="0,0,0,8"
                                   FontWeight="Semibold"
                                   Text="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.Advanced_GLC_WarningAdmin, Mode=OneWay}" />
                        <TextBlock Margin="0,0,8,8"
                                   Style="{ThemeResource SubtitleTextBlockStyle}"
                                   Text="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.Advanced_GLC_PostExit_Title, Mode=OneWay}" />
                        <ToggleSwitch Margin="0,0,0,8"
                                      extension:UIElementExtensions.CursorType="Hand"
                                      Header="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.Advanced_GLC_PostExit_Subtitle, Mode=OneWay}"
                                      IsOn="{x:Bind IsUsePostExitCommand, Mode=TwoWay}"
                                      OffContent="{x:Bind helper:Locale.Current.Lang._Misc.Disabled, Mode=OneWay}"
                                      OnContent="{x:Bind helper:Locale.Current.Lang._Misc.Enabled, Mode=OneWay}" />
                        <TextBox x:Name="PostExitCommandTextBox"
                                 Margin="0,0,0,4"
                                 HorizontalAlignment="Stretch"
                                 CornerRadius="8,8,0,0"
                                 IsSpellCheckEnabled="False"
                                 Text="{x:Bind PostExitCommand, Mode=TwoWay}" />
                        <TextBlock Margin="0,0,0,16"
                                   FontWeight="Semibold"
                                   Text="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.Advanced_GLC_WarningAdmin, Mode=OneWay}" />
                        <TextBlock Margin="0,0,8,8"
                                   Style="{ThemeResource SubtitleTextBlockStyle}"
                                   Text="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.Advanced_RunWithExplorerAsParent_Title, Mode=OneWay}" />
                        <ToggleSwitch Margin="0,0,0,0"
                                      Header="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.Advanced_RunWithExplorerAsParent_Subtitle, Mode=OneWay}"
                                      IsOn="{x:Bind RunWithExplorerAsParent, Mode=TwoWay}"
                                      OffContent="{x:Bind helper:Locale.Current.Lang._Misc.Disabled, Mode=OneWay}"
                                      OnContent="{x:Bind helper:Locale.Current.Lang._Misc.Enabled, Mode=OneWay}" />
                        <TextBlock Margin="0,0,8,8"
                                   FontWeight="Semibold"
                                   Text="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.Advanced_RunWithExplorerAsParent_Warning, Mode=OneWay}" />
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
            <Grid x:Name="GameSettingsApplyGrid"
                  Margin="16"
                  Padding="16,16"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Bottom"
                  Background="{ThemeResource GameSettingsApplyGridBrush}"
                  CornerRadius="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button x:Name="ApplyButton"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        extension:UIElementExtensions.CursorType="Hand"
                        Click="ApplyButton_Click"
                        CornerRadius="16"
                        IsEnabled="True"
                        Style="{ThemeResource AccentButtonStyle}">
                    <StackPanel Margin="8,0"
                                Orientation="Horizontal">
                        <FontIcon FontFamily="{ThemeResource FontAwesomeSolid}"
                                  FontSize="14"
                                  Glyph="&#xf00c;" />
                        <TextBlock Margin="8,0,0,0"
                                   FontWeight="Medium"
                                   Text="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.ApplyBtn, Mode=OneWay}" />
                    </StackPanel>
                </Button>
                <TextBlock x:Name="ApplyText"
                           Grid.Column="1"
                           Margin="16,-4,0,0"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Center"
                           Style="{ThemeResource BodyStrongTextBlockStyle}"
                           Text="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.SettingsApplied, Mode=OneWay}"
                           TextWrapping="Wrap"
                           Visibility="Collapsed" />
                <StackPanel Grid.Column="2"
                            HorizontalAlignment="Right"
                            Orientation="Horizontal">
                    <TextBlock Margin="16,-4,16,0"
                               VerticalAlignment="Center"
                               FontWeight="Medium"
                               Text="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.RegImportExport, Mode=OneWay}" />
                    <Button x:Name="RegistryExport"
                            Height="32"
                            extension:UIElementExtensions.CursorType="Hand"
                            Click="RegistryExportClick"
                            CornerRadius="16,0,0,16"
                            Style="{ThemeResource AccentButtonStyle}"
                            ToolTipService.ToolTip="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.RegExportTooltip, Mode=OneWay}">
                        <StackPanel Margin="8,0"
                                    Orientation="Horizontal">
                            <FontIcon FontFamily="{ThemeResource FontAwesomeSolid}"
                                      FontSize="14"
                                      Glyph="&#xf56e;" />
                            <TextBlock Margin="8,0,0,0"
                                       FontWeight="Medium"
                                       Text="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.RegExportTitle, Mode=OneWay}" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="RegistryImport"
                            Height="32"
                            extension:UIElementExtensions.CursorType="Hand"
                            Click="RegistryImportClick"
                            CornerRadius="0,16,16,0"
                            Style="{ThemeResource AccentButtonStyle}"
                            ToolTipService.ToolTip="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.RegImportTooltip, Mode=OneWay}">
                        <StackPanel Margin="8,0"
                                    Orientation="Horizontal">
                            <TextBlock Margin="0,0,8,0"
                                       FontWeight="Medium"
                                       Text="{x:Bind helper:Locale.Current.Lang._GameSettingsPage.RegImportTitle, Mode=OneWay}" />
                            <FontIcon FontFamily="{ThemeResource FontAwesomeSolid}"
                                      FontSize="14"
                                      Glyph="&#xf56f;" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
        <Grid x:Name="Overlay"
              Visibility="Collapsed">
            <StackPanel Margin="0,176,0,0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Orientation="Vertical">
                <ProgressRing x:Name="Ring"
                              Width="48"
                              Height="48"
                              Margin="32"
                              IsActive="True"
                              IsIndeterminate="false"
                              Maximum="100"
                              Value="100" />
                <TextBlock x:Name="OverlayTitle"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Style="{ThemeResource SubtitleTextBlockStyle}"
                           Text="Title" />
                <TextBlock x:Name="OverlaySubtitle"
                           Margin="0,8,0,192"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Style="{ThemeResource BodyStrongTextBlockStyle}"
                           Text="Subtitle" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
