<!--  ReSharper disable IdentifierTypo  -->
<!--  ReSharper disable UnusedMember.Local  -->
<!--  ReSharper disable Xaml.ConstructorWarning  -->
<Page x:Class="CollapseLauncher.Pages.OOBE.OOBESelectGame"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:helper="using:CollapseLauncher.Helper"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:extension="using:CollapseLauncher.Extension"
      xmlns:pages="using:CollapseLauncher.Pages"
      xmlns:metadata="using:CollapseLauncher.Helper.Metadata"
      Background="Transparent"
      mc:Ignorable="d">
    <Page.Resources>
        <pages:GameTitleComboBoxTranslatedConverter x:Key="GameTitleComboBoxTranslatedConverter"/>
        <pages:GameRegionComboBoxTranslatedConverter x:Key="GameRegionComboBoxTranslatedConverter"/>
        <pages:StringUppercaseConverter x:Key="StringUppercaseConverter"/>
        <pages:GameChannelToVisibilityConverter x:Key="GameChannelToVisibilityConverter"/>
    </Page.Resources>
    <Grid>
        <Frame x:Name="BackgroundFrame"
               HorizontalAlignment="Stretch"
               VerticalAlignment="Stretch" />
        <Button x:Name="NextPage"
                extension:UIElementExtensions.CursorType="Hand"
                Margin="32"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Click="NextPage_Click"
                IsEnabled="False"
                Opacity="0"
                Style="{ThemeResource TransparentDefaultButtonStyle}">
            <Button.OpacityTransition>
                <ScalarTransition />
            </Button.OpacityTransition>
            <Button.Content>
                <StackPanel>
                    <TextBlock FontSize="16"
                               FontStretch="UltraExpanded"
                               FontWeight="SemiBold"
                               Text="{x:Bind helper:Locale.Current.Lang._StartupPage.Pg2NextBtn}" />
                    <AnimatedIcon Width="24"
                                  MinHeight="{ThemeResource ComboBoxMinHeight}"
                                  Margin="0,-4,0,-8"
                                  HorizontalAlignment="Center">
                        <animatedvisuals:AnimatedChevronDownSmallVisualSource />
                        <AnimatedIcon.FallbackIconSource>
                            <FontIconSource FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                            FontSize="12"
                                            Foreground="{ThemeResource ComboBoxDropDownGlyphForeground}"
                                            Glyph="&#xE70D;" />
                        </AnimatedIcon.FallbackIconSource>
                    </AnimatedIcon>
                </StackPanel>
            </Button.Content>
        </Button>
        <Button x:Name="PrevPage"
                extension:UIElementExtensions.CursorType="Hand"
                Margin="32"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Click="PrevPage_Click"
                IsEnabled="True"
                Style="{ThemeResource TransparentDefaultButtonStyle}">
            <Button.OpacityTransition>
                <ScalarTransition />
            </Button.OpacityTransition>
            <Button.Content>
                <StackPanel>
                    <Grid HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          RenderTransformOrigin="0.5, 0.5">
                        <Grid.RenderTransform>
                            <RotateTransform Angle="180" />
                        </Grid.RenderTransform>
                        <AnimatedIcon Width="24"
                                      MinHeight="{ThemeResource ComboBoxMinHeight}"
                                      Margin="0,-8,0,-4"
                                      HorizontalAlignment="Center">
                            <animatedvisuals:AnimatedChevronDownSmallVisualSource />
                            <AnimatedIcon.FallbackIconSource>
                                <FontIconSource FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                                FontSize="12"
                                                Foreground="{ThemeResource ComboBoxDropDownGlyphForeground}"
                                                Glyph="&#xE70D;" />
                            </AnimatedIcon.FallbackIconSource>
                        </AnimatedIcon>
                    </Grid>
                    <TextBlock FontSize="16"
                               FontStretch="UltraExpanded"
                               FontWeight="SemiBold"
                               Text="{x:Bind helper:Locale.Current.Lang._StartupPage.Pg2PrevBtnNew}" />
                </StackPanel>
            </Button.Content>
        </Button>
        <Grid x:Name="Bg"
              Margin="32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <StackPanel x:Name="RightPanel"
                        Grid.Column="0"
                        Margin="32,0,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center">
                <StackPanel.Transitions>
                    <EntranceThemeTransition />
                </StackPanel.Transitions>
                <TextBlock Margin="0,0,0,32"
                           Foreground="{ThemeResource SystemAccentColor}"
                           Style="{ThemeResource TitleLargeTextBlockStyle}"
                           Text="{x:Bind helper:Locale.Current.Lang._StartupPage.Pg2Title}" />
                <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}">
                    <Run FontWeight="Normal"
                         Text="{x:Bind helper:Locale.Current.Lang._StartupPage.Pg2Subtitle1_1}" />
                    <Run FontWeight="Bold"
                         Foreground="{ThemeResource SystemAccentColor}"
                         Text="{x:Bind helper:Locale.Current.Lang._StartupPage.Pg2Subtitle1_2}" />
                    <Run FontWeight="Normal"
                         Text="{x:Bind helper:Locale.Current.Lang._StartupPage.Pg2Subtitle1_3}" />
                </TextBlock>
                <TextBlock Margin="0,16,0,24"
                           Style="{ThemeResource SubtitleTextBlockStyle}">
                    <Run FontWeight="Normal"
                         Text="{x:Bind helper:Locale.Current.Lang._StartupPage.Pg2Subtitle2_1}" />
                    <Run Foreground="{ThemeResource SystemAccentColor}"
                         Text="{x:Bind helper:Locale.Current.Lang._StartupPage.Pg2Subtitle2_2}" />
                    <Run FontWeight="Normal"
                         Text="{x:Bind helper:Locale.Current.Lang._StartupPage.Pg2Subtitle2_3}" />
                    <Run FontWeight="Bold"
                         Foreground="{ThemeResource SystemAccentColor}"
                         Text="{x:Bind helper:Locale.Current.Lang._StartupPage.Pg2Subtitle2_4}" />
                    <Run FontWeight="Normal"
                         Text="{x:Bind helper:Locale.Current.Lang._StartupPage.Pg2Subtitle2_5}" />
                </TextBlock>
                <StackPanel Orientation="Horizontal">
                    <ComboBox x:Name="GameTitleComboBox"
                              extension:UIElementExtensions.CursorType="Hand"
                              Width="192"
                              CornerRadius="16"
                              PlaceholderText="{x:Bind helper:Locale.Current.Lang._StartupPage.Pg2ComboBox}"
                              SelectionChanged="GameCategorySelect_SelectionChanged">
                        <ComboBox.ItemTemplate>
                            <DataTemplate x:DataType="x:String">
                                <ContentPresenter Content="{Binding Converter={StaticResource GameTitleComboBoxTranslatedConverter}}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <ComboBox x:Name="GameRegionComboBox"
                              extension:UIElementExtensions.CursorType="Hand"
                              Width="192"
                              Margin="16,0,0,0"
                              CornerRadius="16"
                              IsEnabled="False"
                              PlaceholderText="{x:Bind helper:Locale.Current.Lang._StartupPage.Pg2ComboBoxRegion}"
                              SelectionChanged="GameSelect_SelectionChanged">
                        <ComboBox.ItemTemplate>
                            <DataTemplate x:DataType="metadata:PresetConfig">
                                <StackPanel Orientation="Horizontal"
                                            Spacing="4">
                                    <ContentPresenter Content="{Binding Converter={StaticResource GameRegionComboBoxTranslatedConverter}}"/>
                                    <Grid Background="{ThemeResource TextBoxBackgroundThemeBrush}"
                                          CornerRadius="4"
                                          Padding="4, 0"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"
                                          Visibility="{x:Bind GameChannel, Mode=OneWay, Converter={StaticResource GameChannelToVisibilityConverter}}">
                                        <TextBlock Text="{x:Bind GameChannel, Mode=OneWay, Converter={StaticResource StringUppercaseConverter}}"
                                                   FontSize="10"
                                                   FontWeight="Bold"
                                                   VerticalAlignment="Center"
                                                   HorizontalAlignment="Center"
                                                   Foreground="{ThemeResource TextBoxForegroundThemeBrush}"/>
                                    </Grid>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
            </StackPanel>
        </Grid>
        <Grid x:Name="Overlay"
              Visibility="Collapsed">
            <StackPanel Margin="0,200,0,0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Orientation="Vertical">
                <ProgressRing x:Name="Ring"
                              Width="48"
                              Height="48"
                              Margin="32"
                              IsActive="True"
                              IsIndeterminate="false"
                              Maximum="100"
                              Value="100" />
                <TextBlock x:Name="OverlayTitle"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Style="{ThemeResource SubtitleTextBlockStyle}"
                           Text="Title" />
                <TextBlock x:Name="OverlaySubtitle"
                           Margin="0,8,0,192"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Style="{ThemeResource BodyStrongTextBlockStyle}"
                           Text="Subtitle" />
            </StackPanel>
        </Grid>
        <ProgressBar x:Name="BarBGLoading"
                     VerticalAlignment="Bottom"
                     IsIndeterminate="true"
                     Visibility="Collapsed" />
    </Grid>
</Page>
